/* ============================================================
   CITY.JS — City detail page
   Itinerary timeline, places CRUD, food CRUD, tips CRUD
   Built in a future session.
   ============================================================ */

export function initCity(cityName) {
  // TODO
}
